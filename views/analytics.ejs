<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Sniplyx - Analytics</title>
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(to right, #0f2027, #203a43, #2c5364);
      color: #fff;
    }

    .navbar {
      background-color: rgba(255, 255, 255, 0.05);
    }

    .analytics-box {
      background: #fff;
      color: #333;
      border-radius: 16px;
      padding: 2rem;
      max-width: 700px;
      margin-top: 80px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
    }

    .click-list li {
      background-color: #f8f9fa;
      border-radius: 8px;
      margin-bottom: 8px;
      padding: 10px;
      font-size: 0.95rem;
    }

    .btn-back {
      margin-top: 20px;
    }
  </style>
</head>
<body>

  <!-- ✅ Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark px-3">
    <div class="container-fluid">
      <span class="navbar-brand fw-bold fs-4 text-primary">Sniplyx</span>
    </div>
  </nav>

  <!-- ✅ Main Container -->
  <div class="container d-flex justify-content-center">
    <div class="analytics-box text-center">
      <h2 class="mb-3 text-primary">📊 Link Analytics</h2>

      <p class="lead mb-4">
        <strong>Total Clicks:</strong> <span class="badge bg-success fs-5"><%= totalClicks %></span>
      </p>

      <h5 class="text-secondary mb-3">Click History:</h5>

      <% if (analytics.length > 0) { %>
        <ul class="list-unstyled click-list">
          <% analytics.forEach(item => { %>
            <li><%= new Date(item.timestamp).toLocaleString() %></li>
          <% }) %>
        </ul>
      <% } else { %>
        <p class="text-muted">No clicks recorded yet for this short URL.</p>
      <% } %>

      <a href="/" class="btn btn-outline-light btn-back">← Back to Home</a>
    </div>
  </div>

</body>
</html>
